## What & why

<!-- One or two sentences: what does this change, and why? -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run parity` passes
- [ ] If the status-line output changed on purpose, I re-blessed the goldens (`npm run parity -- --bless`) and explained why above
- [ ] No new dependencies (the kit is zero-dependency, pure Node)
- [ ] One focused change (unrelated changes split into separate PRs)
